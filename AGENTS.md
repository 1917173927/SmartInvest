# Codex 开发约定

本文件约束 Codex 在本仓库中的开发行为。默认由 Codex 独立完成分析、实现、测试、审查与
Git 交付，不调用外部编码 Agent。

## 开始工作前

1. 先阅读 `README.md` 与 `ARCHITECTURE.md`，再检查 `git status --short` 和最近提交。
2. 工作区中的既有改动默认属于用户；不得擅自覆盖、回滚、暂存或提交。
3. 延续当前实现风格：核心计算保持确定性，外部数据访问留在数据边界，可选依赖必须能够
   降级，新增行为必须配套测试。
4. 不得把开发协作逻辑实现成产品运行时能力；`ARCHITECTURE.md` 中 v1 的产品边界继续有效。

## 实现与数据安全

1. 修改前先定位真实数据流和调用边界，避免只修正文案而不修正计算口径。
2. 公共模型（如 `StagingPlan`、`ValuationRange`、`AnalysisPackage`、`AppConfig`）变更时，
   必须同步检查所有调用点和序列化兼容性。
3. 测试必须使用 `pytest` 的 `tmp_path` 临时数据库，严禁向生产数据库
   `.stock-analysis/analysis.sqlite3` 写入测试数据。
4. 行情或金融计算必须显示数据来源、时间和降级状态；过期或失败的数据不得伪装成实时数据。
5. 仓位建议必须同时检查账户总资产、已有持仓、目标上限、A 股整手约束和可用现金核验提示。

## Git 约定

- Codex 分支默认使用 `codex/<简短任务名>`。
- 提交保持小而完整，一个提交只表达一个目的，不混入用户改动、无关格式化或生成文件。
- Git 提交主题必须使用中文，并保留 Conventional Commit 类型前缀，例如：
  - `feat: 增加行业估值中枢配置`
  - `fix: 修复盘前挂单边界计算`
  - `test: 补充组合风控回归测试`
  - `docs: 完善实盘操作说明`
- 未经用户明确要求，不执行强制推送、历史重写或破坏性 Git 操作。

## 验证与交付门禁

提交前必须执行并通过：

```bash
uv run ruff format --check .
uv run ruff check .
uv run pytest
```

若完整测试受外部数据、模型权重或可选依赖限制，运行覆盖改动的最小测试集，并在交付说明中
明确未运行项目及原因。最终交付说明修改内容、验证结果、提交哈希和推送状态。
