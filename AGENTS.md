# Agent 开发协作约定

本文件约束所有在本仓库中工作的开发 Agent。目标是保持现有代码的风格、架构边界和可审计性，并让两个 Agent 可以并行工作而不互相覆盖。

## 开始工作前

1. 先阅读 `README.md` 与 `ARCHITECTURE.md`，再检查 `git status --short` 和最近提交。
2. 工作区中的既有改动默认属于用户或另一个 Agent；不得擅自覆盖、回滚、暂存或提交。
3. 延续当前实现风格：核心计算保持确定性，外部数据访问留在数据边界，可选依赖必须能够降级，新增行为必须配套测试。
4. 不得把开发阶段的多 Agent 协作实现成产品运行时能力；`ARCHITECTURE.md` 中 v1 的产品边界继续有效。

## 双 Agent 协作协议

两个 Agent 不得在同一个工作目录中并行写文件。默认采用“一名集成 Agent + 一名实现 Agent”的模式：

- **集成 Agent**：拆分任务、定义接口和文件所有权、保留主工作区、最终审查与合并。
- **实现 Agent**：只在独立 Git worktree 和独立分支中完成边界清晰的子任务，提交后把提交哈希交给集成 Agent。

开始并行前，双方必须明确记录以下内容：

| 项目 | 必须明确的内容 |
|---|---|
| 任务目标 | 可独立验收的一句话结果 |
| 文件所有权 | 本轮允许修改的文件或目录 |
| 接口契约 | 函数签名、数据结构、异常和兼容性要求 |
| 验证命令 | 至少包含相关测试和 Ruff 检查 |
| 合并顺序 | 谁先提交，谁负责解决集成冲突 |

协作时遵守以下硬规则：

1. 同一时刻，一个文件只能有一个写入者。若两个任务必须修改同一文件，取消并行，按约定顺序串行完成。
2. 测试文件也属于文件所有权的一部分，不能默认由双方同时修改。
3. 跨模块协作先确定接口契约，再分别实现；不得在对方工作期间单方面改变已约定的接口。
4. 实现 Agent 不直接合并、变基或修改集成 Agent 的分支；集成 Agent 通过 `cherry-pick` 或常规合并吸收已完成提交。
5. 合并前，集成 Agent 先确认主工作区没有非预期改动，再逐个吸收提交并运行完整验证。
6. 若发现所有权重叠、接口需要变更或已有未提交改动，立即暂停相关文件的写入，先重新划分边界。

推荐的分工优先级是：不同模块并行 > 实现与测试夹具分离 > 代码与文档分离。不要为了并行而强行拆分一个高度耦合的修改。

## 分支与提交

- 每个并行任务使用独立分支，Codex 分支默认使用 `codex/<简短任务名>`。
- 提交保持小而完整，一个提交只表达一个目的，不混入无关格式化或生成文件。
- Git 提交主题必须使用中文，并保留 Conventional Commit 类型前缀，例如：
  - `feat: 增加行业估值中枢配置`
  - `fix: 修复盘前挂单边界计算`
  - `test: 补充组合风控回归测试`
  - `docs: 完善双 Agent 协作约定`
- 未经用户明确要求，不执行推送、强制推送、历史重写或破坏性 Git 操作。

## 验证与交付

修改 Python 代码后至少运行：

```bash
uv run ruff check .
uv run pytest
```

若完整测试受外部数据、模型权重或可选依赖限制，运行能够覆盖改动的最小测试集，并在交付说明中明确未运行的项目及原因。

最终交付应说明：修改了什么、验证结果、提交哈希，以及是否存在需要另一名 Agent 串行处理的文件。
