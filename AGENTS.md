# Agent 开发协作约定

本文件约束所有在本仓库中工作的开发 Agent（如 Gemini 与 GPT/Codex）。目标是保持现有代码的风格、架构边界和可审计性，让两个 Agent 可以高效并行工作而不发生代码覆盖或运行时冲突。

---

## 🎯 Gemini 与 GPT 推荐角色与分工矩阵

基于大模型特长与本系统架构（Python CLI + Obsidian 知识库 + 本地 SQLite + 时序量化模型），推荐分工如下：

| 维度 | Gemini (Antigravity) 推荐职责 | GPT (Codex) 推荐职责 |
|---|---|---|
| **协作定位** | **集成 Agent (Integration & Architecture)** | **实现 Agent (Implementation & Algorithms)** |
| **工作环境** | 保留主工作区（`/Users/b./StockAnalysis`） | 独立 Worktree（`../StockAnalysis-wt-<任务名>`） |
| **核心优势** | 超大上下文全景理解、多模态视觉审计、知识库全局链接 | 强代码逻辑推理、专注算法闭式推导、独立模块精细编码 |
| **负责领域** | 1. 用户交互与多模态持仓截图/财报视觉审计<br>2. 任务拆分、定义公共接口契约与文件所有权<br>3. Obsidian 知识库全局组织、渲染器与双向链接维护<br>4. 吸收提交（cherry-pick）、解决冲突与全量回归验收 | 1. 独立量化金融指标/公式算法实现（如波动率、动量）<br>2. 独立 CLI 命令与子系统模块化实现<br>3. 单元测试用例与测试夹具（Fixtures）编写<br>4. 输出自测完备的原子 Commit Hash 交付集成 |

---

## 开始工作前

1. 先阅读 `README.md` 与 `ARCHITECTURE.md`，再检查 `git status --short` 和最近提交。
2. 工作区中的既有改动默认属于用户或另一个 Agent；不得擅自覆盖、回滚、暂存或提交。
3. 延续当前实现风格：核心计算保持确定性，外部数据访问留在数据边界，可选依赖必须能够降级，新增行为必须配套测试。
4. 不得把开发阶段的多 Agent 协作实现成产品运行时能力；`ARCHITECTURE.md` 中 v1 的产品边界继续有效。

---

## 双 Agent 协作协议与硬规则

两个 Agent 不得在同一个工作目录中并行写文件。默认采用“一名集成 Agent + 一名实现 Agent”的模式：

- **集成 Agent**：拆分任务、定义接口和文件所有权、保留主工作区、最终审查与合并。
- **实现 Agent**：只在独立 Git worktree 和独立分支中完成边界清晰的子任务，提交后把提交哈希交给集成 Agent。

### 1. 任务准备（明确记录清单）
开始并行前，双方必须明确记录以下内容：

| 项目 | 必须明确的内容 |
|---|---|
| 任务目标 | 可独立验收的一句话结果 |
| 文件所有权 | 本轮允许修改的文件或目录 |
| 接口契约 | 函数签名、公共 Dataclass、异常和兼容性要求 |
| 验证命令 | 至少包含相关单元测试和 Ruff 检查 |
| 合并顺序 | 谁先提交，谁负责解决集成冲突 |

### 2. 协作硬性执行纪律
1. **单文件独占写入**：同一时刻，一个文件只能有一个写入者。若两个任务必须修改同一文件，取消并行，按约定顺序串行完成。
2. **测试文件所有权隔离**：测试文件也属于文件所有权的一部分，不能默认由双方同时修改。
3. **公共 Dataclass 与契约熔断**：公共模型（如 `StagingPlan`, `ValuationRange`, `AnalysisPackage`, `AppConfig`）所有权归集成 Agent。实现 Agent 若在开发中发现数据结构需要变更，**必须立即暂停写入并提出变更提案**，经集成 Agent 确认后方可继续。
4. **Git Worktree 目录防污染**：
   - 实现 Agent 的 worktree 必须建立在**主仓库根目录之外**（例如 `../StockAnalysis-wt-<任务名>`），严禁建在仓库内部，防止污染 Obsidian 知识库索引；
   - 集成 Agent 吸收提交后，必须显式执行 `git worktree remove` 清理。
5. **SQLite 本地数据库并发防死锁**：
   - 实现 Agent 在独立分支开发和跑测试时，必须严格使用 `pytest` 的 `tmp_path` 临时测试数据库，**严禁在开发测试中直接向生产数据库 `.stock-analysis/analysis.sqlite3` 写入持久化数据**。
6. **吸收合并规则**：实现 Agent 不直接合并、变基或修改主分支；集成 Agent 确认主工作区无非预期改动后，通过 `cherry-pick` 或常规合并吸收提交并执行完整验收。

---

## 分支与提交

- 每个并行任务使用独立分支，Codex 分支默认使用 `codex/<简短任务名>`。
- 提交保持小而完整，一个提交只表达一个目的，不混入无关格式化或生成文件。
- Git 提交主题必须使用中文，并保留 Conventional Commit 类型前缀，例如：
  - `feat: 增加行业估值中枢配置`
  - `fix: 修复盘前挂单边界计算`
  - `test: 补充组合风控回归测试`
  - `docs: 完善双 Agent 协作约定`
- 未经用户明确要求，不执行强制推送、历史重写或破坏性 Git 操作。

---

## 验证与交付门禁

实现 Agent 提交前及集成 Agent 合并后，**必须强制执行并通过**：

```bash
# 1. 格式与 Lint 零容忍检查（遵循 pyproject.toml 100 字符行长限制）
uv run ruff format --check .
uv run ruff check .

# 2. 全量或最小闭环单元测试
uv run pytest
```

若完整测试受外部数据、模型权重或可选依赖限制，运行能够覆盖改动的最小测试集，并在交付说明中明确未运行的项目及原因。

最终交付应说明：修改了什么、验证结果、提交哈希，以及是否存在需要另一名 Agent 串行处理的文件。
